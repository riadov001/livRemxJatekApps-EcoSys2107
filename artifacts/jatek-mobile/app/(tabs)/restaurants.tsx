import { Redirect } from "expo-router";

// Same content as Home for now — full design will follow once the Home is approved.
export default function RestaurantsTab() {
  return <Redirect href="/(tabs)" />;
}
